import UIKit

var edad = 16
var cantidadDinero = 100
var sexoMasculino = false

if (edad > 17 || cantidadDinero > 50) && (sexoMasculino){
    
    print("Cumplis con los requisitos y podes pasar al boliche")
} else {
    print("No complis los requisitos para pasar al boliche")
}
